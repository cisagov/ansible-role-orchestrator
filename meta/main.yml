---
# Note that dependencies listed here are automatically installed
# before this role.  Role variables for any roles listed here can be
# assigned static variables.
#
# See also cisagov/skeleton-ansible-role#153.
dependencies:
  - name: docker
    src: https://github.com/cisagov/ansible-role-docker
galaxy_info:
  author: Shane Frasier
  company: CISA Cyber Assessments
  description: Ansible role for installing cisagov/orchestrator
  galaxy_tags: []
  license: CC0-1.0
  # With the release of version 2.10, Ansible finally correctly
  # identifies Kali Linux as being the Kali distribution of the Debian
  # OS family.  This simplifies a lot of things for roles that support
  # Kali Linux, so it makes sense to force the installation of Ansible
  # 2.10 or newer.
  min_ansible_version: "2.10"
  namespace: cisagov
  platforms:
    - name: Amazon Linux
      versions:
        - "2023"
    - name: Debian
      versions:
        - buster
        - bullseye
        - bookworm
        - trixie
        # TODO: Docker does not yet officially support Debian Forky,
        # but we should add support when that becomes possible.  See
        # https://docs.docker.com/engine/install/debian/ for more
        # details.
        # - forky
    - name: Fedora
      versions:
        - "42"
        - "43"
    - name: Kali
      versions:
        - "2023"
    - name: Ubuntu
      versions:
        - jammy
        - noble
        - resolute
  role_name: orchestrator
  standalone: true
